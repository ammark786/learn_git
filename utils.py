"""Utility helpers for Git rebase practice."""


def greet(name: str) -> str:
    """Return a greeting string."""
    return f"Hello Welcome, {name}!"


def format_number(n: float) -> str:
    """Format a number with 2 decimal places."""
    return f"{n:.2f}"
